[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

[functions.webhook]
  timeout = 300  # 5 minutes (max for Pro plan)

[[redirects]]
  from = "/webhook/trello"
  to = "/.netlify/functions/webhook/webhook/trello"
  status = 200

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/webhook/:splat"
  status = 200
